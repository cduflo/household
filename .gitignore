# Nothing secret belongs in this repo — it is public and GitHub Pages serves
# every file in it. The Supabase anon key IS meant to be here; RLS is what
# protects the data. Never add a service_role key or a DB password.
.DS_Store
node_modules/
